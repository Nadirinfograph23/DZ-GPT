// YouTube Insight Controller — stub

export async function handleYouTubeInput(url, opts = {}) {
  return { ok: false, error: 'YouTube Insight not available in this environment.', url }
}

export async function handleVideoDiscussion(url, question, opts = {}) {
  return { ok: false, error: 'YouTube Insight not available in this environment.', url }
}
