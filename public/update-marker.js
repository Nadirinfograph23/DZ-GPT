// Production update marker. The deployment pipeline should set VITE_BUILD_ID
// (normally to the Git commit SHA) so every GitHub deployment is distinguishable.
window.__DZAGENT_BUILD_ID__ = window.__DZAGENT_BUILD_ID__ || '';
