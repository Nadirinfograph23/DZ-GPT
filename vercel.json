{
  "buildCommand": "npm run build && cp server.js api/ && for d in lib modules dialect data bin; do [ -d \"$d\" ] && cp -r \"$d\" api/ || true; done",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
