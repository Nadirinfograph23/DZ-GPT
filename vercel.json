{
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "npm run build && npx esbuild server.js --bundle --platform=node --target=node20 --format=esm --outfile=api/server-bundle.js --packages=external --external:fsevents --external:bufferutil --external:utf-8-validate --log-level=warning",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/index.js": {
      "maxDuration": 300,
      "includeFiles": "bin/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}